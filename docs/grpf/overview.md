# GRPF – Runtime Oracle Report Parameter Form Builder with AI-Assisted Design

Generative Report Parameter Form AI is a runtime-driven Oracle Forms solution designed to create, configure, and deploy report parameter forms without separate `.fmb` development for each form.

Its central idea is simple: use one reusable runtime engine and database-driven metadata to generate forms dynamically.
